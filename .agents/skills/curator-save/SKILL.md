---
name: curator-save
description: Сохраняет проверенные знания из сессии в базу знаний через Memory Curator. Использовать по явной просьбе («сохрани знания», /curator-save) И проактивно: когда в ходе сессии выяснено проверенное абстрактное знание (правило, паттерн, принцип) — предложить сохранить, не дожидаясь просьбы.
---

# Curator Save

Сохранение знаний из сессии в базу через MCP-сервер memory-curator.

## Когда срабатывать

- Явная просьба: «сохрани знания», «запомни это», `/curator-save`
- **Проактивно**: в ходе сессии выяснили что-то проверённое (подтверждено
  исходниками, логами или устройством) и абстрактное (не привязано к
  конкретной задаче) — предложи: «Выяснили проверенное знание — сохранить
  в базу?». Не сохраняй без подтверждения пользователя.

## Что сохранять, а что нет

- Да: проверенные правила, паттерны, принципы — подтверждённые и абстрактные
- Нет: фичевые детали, конкретные баги, неподтверждённые гипотезы,
  проект-специфичные детали

## Шаги

1. Извлеки из сессии кандидатов: для каждого — `type`
   (Reference | Style | Tool | Spec), `title` (одна строка, ≥10 знаков),
   `content_summary` (1-2 предложения), `tags`, `evidence` (цитата из сессии).
   Контракты формата: заголовок — одна строка без `#`; описание — без строк,
   начинающихся с `#` или `*Тип:*`/`*Теги:*`; теги — без `|` и `,`.
   Gatekeeper отклонит нарушителей — не пытайся обойти.
2. Self-review: вызови `curator_query` с релевантыми ключевыми словами;
   убери кандидатов, которые уже есть в базе.
3. Покажи пользователю preview и спроси подтверждение (все / выборочно / отказ).
4. После подтверждения вызови `curator_session_capture` с `auto_approve=true`
   и выбранными кандидатами.
