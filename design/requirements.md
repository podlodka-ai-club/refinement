---
type: Hackathon
project: Memory Curator
event: Hacker Sprint #2 (podlodka-ai-club)
date: 2026-08-20 to 2026-09-08
repo: https://github.com/podlodka-ai-club/refinement
---

# Требования хакатона

Извлечено из сообщения в чате команды и страницы правил.

## Дедлайны

| # | Что | Когда |
|---|-----|-------|
| 1 | Выбрать капитана команды | 23 августа 2026 (воскресенье) |
| 2 | Выбрать тему проекта | 23 августа 2026 (воскресенье) |
| 3 | Промежуточная анкета (техстек, компоненты, грабли) | 30 августа 2026 |
| 4 | Финальный пакет: анкета + видеодемка | 8 сентября 2026 |

## Тема трека

**«Агент, который помнит»** — строим фонового агента, который автономно обрабатывает поток однотипных задач и с каждой итерацией становится лучше: извлекает уроки из своих же результатов, копит опыт и переиспользует его в следующих задачах.

## Правила

- Технический стек — любой (модели, агенты — на наш выбор)
- Можно использовать open-source проекты, главное — докрутить своё
- Код в GitHub: `podlodka-ai-club/refinement`
- GitHub аккаунты скидывать в ответ на сообщение в чате
- Можно привести друга не из клуба
- Бонус: использование **xmemory** как memory backend
- Бонус: денежные призы в отдельной номинации
- Не страшно не успеть всё — демку всё равно стоит сделать
- Организационные/технические вопросы → комната Hacker Sprint в общем чате, либо тегать @etolstoy

## Ключевые фразы из правил (для самопроверки)

- «автономно обрабатывает поток однотипных задач»
- «с каждой итерацией становится лучше»
- «извлекает уроки из своих же результатов»
- «копит опыт»
- «переиспользует его в следующих задачах»

---

## Минимальные требования (MUST HAVE)

> Из официальных правил Hacker Sprint #2

| # | Требование | Memory Curator | Статус |
|---|-----------|----------------|--------|
| 1 | Обрабатывать поток задач из выбранного источника | `.md` файлы в `learnings/` (ingest) + сессии харнесов (candidates от агента) | ✅ |
| 2 | Цикл «выполнил → оценил → извлёк урок» | Агент харнеса извлекает → `curator_session_capture(candidates)` → `gatekeeper.py` → backend (xmemory/SQLite) → `improve_loop.py` | ✅ |
| 3 | Менять поведение на основе опыта | Improve loop находит дубликаты → consolidation; stale → deprecation; eval gate проверяет перед изменением | ✅ |
| 4 | Хранить память между рестартами | xmemory (primary) + SQLite (fallback, персистентный файл + offline-outbox, `curator sync`) | ✅ |
| 5 | Поработать с реальными данными и обучиться на них | ingest реальных `.md` из `learnings/` + реальные сессии OpenCode (`session_reader.py`, opencode.db) + фикстуры структуры learnings в тестах | ✅ |
| 6 | Демонстрация дельты «до/после» | `demo.py`: clean (пустая память) одобряет извлечённые факты, trained (обученная) отклоняет их как дубликаты — один и тот же прогон сессий | ✅ |

---

## Дополнительные блоки (NICE TO HAVE)

> Из официальных правил

| Блок | Описание | Memory Curator | Статус |
|------|----------|----------------|--------|
| Забывание и устаревание | Обесценивать или выкидывать устаревший опыт | Семантическое устаревание: `improve_loop._find_stale()` (hypothesis → deprecated по eval-гейту) + проигравшие противоречий. Таймерного decay по неиспользованию нет осознанно (время не делает факт ложным): телеметрия использования — observability, решение за человеком | ✅ |
| Разрешение противоречий | Новый урок конфликтует со старым — какой победит | `improve_loop._find_contradictions()` + `_pick_winner()`: verified beats hypothesis, больше тегов > меньше, подробнее описание. Автоматическое разрешение | ✅ |
| Эвалы для изменений поведения | Прежде чем менять — проверить что полезно | `eval_runner.py` — gate перед consolidation/deprecation, метрики coverage/staleness | ✅ |
| Human-in-the-loop | Человек подтверждает/правит/отклоняет уроки | `gatekeeper.py` + `auto_mode: false` (default) | ✅ |
| Observability | Видно какой урок к какому изменению привёл | `observability.py` — JSONL лог всех improve-действий с трейсом «урок X → изменение Y → результат Z» | ✅ |

---

## Критерии оценки (общая номинация)

> Из официальных правил

| Критерий | Что значит для нас |
|----------|-------------------|
| Насколько хорошо агент справляется с задачей | Качество извлечённых фактов (gatekeeper), полезность query-ответов, отсутствие дубликатов |
| Дельта «до» и «после» обучения | `demo.py`: пустая память vs обученная — разные результаты одного и того же прогона сессий |
| Количество дополнительных идей | 5 из 5 доп. блоков реализованы (stale, противоречия, эвалы, human-in-the-loop, observability) |

---

## Критерии номинации xmemory

> От организаторов (xmemory.ai)

| Критерий | Memory Curator | Статус |
|----------|----------------|--------|
| Значимый цикл write → read и durability | Факты сохраняются в xmemory, переживают рестарты, query возвращает их | ✅ |
| Схема под задачу | XMD-схема: Reference + Style + relations (links, contradicts) | ✅ |
| xmemory как часть продукта | Primary backend, не отдельное демо сбоку | ✅ |
| Наглядность результата | `curator_status` показывает рост фактов, `demo.py` показывает дельту: дубликаты отклонены обученным агентом | ✅ |
